Procedure Hello is
	Procedure Hola (asd: In Out Float) is
		asd,fdvfd,asdasd: Integer; 
	Begin
		Put(1); 
	end Hola;
	
	Function Prueba (asd: Out Float; prueba: In Integer) Return Integer 
	is 
		asd,asd1:Float;
		cvzzc,dasf:Integer;
		prueba1,prueab2:Boolean;
	Begin 
		Put(2); 
		asd:= 1.0;
		Return Prueba(1.20+2.0);
	end Prueba;
begin
	Put(1);
	Put("num");
	Get(num);
	for variable in 1..2 loop
		Put(1);
		loop
			Put(1); 
			if x<2 then 
				Put(1); 
				Get(Prueba); 
			end if; 
			exit when x<2; 
		end loop; 
	END loop;
	
	if  x<2 and 1>x+2+3*5 or x/=0 and y<=1 or a>=2 and p=2 then
		Put(1);
	end if;
end Hello;
